function init(){document.body.style.backgroundColor="#CFD8DC",scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,1,1e4),camera.position.x=-45*Math.PI/180,camera.position.y=400,camera.position.z=400,renderer=new THREE.WebGLRenderer({alpha:!0}),renderer.setSize(window.innerWidth/4,window.innerHeight/4,!1),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(function e(){renderer.render(scene,camera),requestAnimationFrame(e)})}function initWorld(){geometry=new THREE.PlaneGeometry(2e3,2e3,20,20),material=new THREE.MeshBasicMaterial({color:16739904}),plane=new THREE.Mesh(geometry,material),plane.rotation.x=-90*Math.PI/180,scene.add(plane),geometry=new THREE.CubeGeometry(1,1,1),geometry.applyMatrix((new THREE.Matrix4).makeTranslation(0,.5,0)),material=new THREE.MeshDepthMaterial;for(var e=0;e<300;e++)rectangle=new THREE.Mesh(geometry.clone(),material.clone()),rectangle.position.x=4*Math.floor(200*Math.random()-100),rectangle.position.z=4*Math.floor(200*Math.random()-100),rectangle.scale.x=50*Math.random()+10,rectangle.scale.y=Math.random()*rectangle.scale.x*8+8,rectangle.scale.z=rectangle.scale.x,scene.add(rectangle)}var scene,camera,renderer,geometry,material,plane,rectangle;init(),initWorld(),animate();