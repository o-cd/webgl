function init(){document.body.style.backgroundColor="#B0BEC5",scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,1,1e4),camera.position.x=-45*Math.PI/180,camera.position.y=400,camera.position.z=400,renderer=new THREE.WebGLRenderer({alpha:!0}),renderer.shadowMap.enabled=!0,renderer.shadowMap.type=THREE.PCFSoftShadowMap,renderer.setSize(window.innerWidth/4,window.innerHeight/4,!1),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(function e(){renderer.render(scene,camera),requestAnimationFrame(e)})}function initWorld(){geometry=new THREE.PlaneGeometry(2e3,2e3,20,20),material=new THREE.MeshBasicMaterial({color:16739904}),plane=new THREE.Mesh(geometry,material),plane.rotation.x=-90*Math.PI/180,plane.receiveShadow=!0,scene.add(plane),geometry=new THREE.CubeGeometry(1,1,1),geometry.applyMatrix((new THREE.Matrix4).makeTranslation(0,.5,0)),material=new THREE.MeshPhongMaterial({color:2503224}),light=new THREE.DirectionalLight(16739904,1),light.position.set(1,3,2),light.castShadow=!0,scene.add(light),scene.fog=new THREE.FogExp2(16739904,.002),rectangles=new THREE.Geometry;for(var e=0;e<300;e++)rectangle=new THREE.Mesh(geometry.clone()),rectangle.position.x=4*Math.floor(200*Math.random()-100),rectangle.position.z=4*Math.floor(200*Math.random()-100),rectangle.scale.x=50*Math.random()+10,rectangle.scale.y=Math.random()*rectangle.scale.x*8+8,rectangle.scale.z=rectangle.scale.x,THREE.GeometryUtils.merge(rectangles,rectangle);world=new THREE.Mesh(rectangles,material),world.castShadow=!0,world.receiveShadow=!0,scene.add(world)}var scene,camera,renderer,geometry,material,light,plane,rectangle,rectangles,world;init(),initWorld(),animate();